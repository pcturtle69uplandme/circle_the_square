.claude/settings.local.json
.env
__pycache__/
*.log
clips/temp_*.mp4
frames/

# photoreal (pre-pivot) videos - kept locally, not in the repo
clips/OPENING_*.mp4
clips/ACTION_TRAILER_MASTER_60S.mp4
clips/PAPER_TRAIL_TRAILER_MASTER_78S.mp4
clips/SMALL_STAKES_TRAILER_MASTER_60S.mp4
clips/CARTOON_BUILDING_TRAILER_2X_FLOW_MUSIC.mp4

# Python virtualenv
.venv/

# Node (Playwright browser-automation toolkit)
node_modules/

# Ad-hoc debug screenshots from browser automation sessions
flow_*.png
cdp_*.png
.playwright-mcp/
scratch/
higgsfield-tools/browser/shots/

# Playwright persistent browser profiles (session cookies -- never commit)
ai.chrome_playwright_profile*/
.chrome_playwright_profile*/
fal-tools/browser/shots/

# Higgsfield CLI vendored skill bundles (reinstalled by the CLI itself, not project content)
.agents/skills/
.claude/skills/
skills-lock.json

# Kimi CLI project config (unrelated coding-tool experiment, not project content)
.kimi-code/

# Never commit credentials -- these sit untracked in clips/cartoon/ and must stay that way
credentials*.txt
*credentials*.txt
