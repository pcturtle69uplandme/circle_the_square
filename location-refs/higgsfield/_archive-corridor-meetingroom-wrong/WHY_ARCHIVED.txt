These 7 plates are NOT the corridor outside Jan's office.

They were generated by generate-location-coverage.js when that script applied one
global ROOM_LAYOUT const -- describing the GLASS MEETING ROOM (goldfish_meeting_room)
-- to every location. So the corridor set came back as meeting-room coverage:
table_head / table_reverse / table_side angles of a room the corridor does not contain.

Only jan_office_corridor_master_wide.png escaped, because the master is text-to-image
and does not chain off the layout text in the same way. It stays in coverage/ and is
the anchor for Scene 2 (see SCENE2_PLATE_SHOT_LIST.md).

Fixed 2026-09-03: LAYOUTS and SHOTS are now per-location in generate-location-coverage.js.
Regenerate the corridor with: node higgsfield-tools/generate-location-coverage.js jan_office_corridor
